#pragma once
#include <vector>
#include "Book.h"

using namespace std;

class Report
	vector<Book*> books;
public:
	int cmpWholesale(Book b1, Book2);

	sort(vector<Book*>)
{
public:
	Report();
	~Report();
};

